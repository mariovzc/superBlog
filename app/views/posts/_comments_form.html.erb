<%= form_for([@comment.post, @comment]) do |f| %>
  <%=render 'shared/errors', object: f.object%>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>